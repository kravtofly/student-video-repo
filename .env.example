# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Mux (Video Platform)
MUX_TOKEN_ID=
MUX_TOKEN_SECRET=
MUX_WEBHOOK_SECRET=        # from Mux webhook settings
MUX_SIGNING_KEY_ID=        # for signed playback tokens
MUX_SIGNING_KEY_SECRET=    # PEM-encoded private key (RSA PKCS1 or PKCS8)
MUX_CORS_ORIGIN=https://www.kravtofly.com  # CORS origin for Mux direct uploads (optional - falls back to first domain in ALLOWED_ORIGINS)

# Stripe (Payments)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET_REVIEW=  # from Stripe webhook endpoint

# App URLs
PUBLIC_APP_URL=https://student-video-repo.vercel.app
PUBLIC_REVIEW_BASE=https://student-video-repo.vercel.app
PUBLIC_STUDENT_PORTAL_URL=https://www.kravtofly.com/my-reviews  # Webflow page where students view their reviews

# CORS (optional - comma-separated list of allowed origins)
ALLOWED_ORIGINS=https://www.kravtofly.com,https://kravtofly.com,http://localhost:3000

# Make.com Webhooks
MAKE_REVIEWED_WEBHOOK_URL=      # webhook URL for review completion notifications
MAKE_STUDENT_MAGIC_WEBHOOK_URL= # webhook URL for magic link emails

# JWT Secret for Magic Links
SVR_JWT_SECRET=  # at least 32 characters for HS256
